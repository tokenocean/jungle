CREATE TABLE "public"."editions" ("id" uuid NOT NULL DEFAULT gen_random_uuid(), "artwork_id" uuid NOT NULL, "edition" integer NOT NULL DEFAULT 1, "asset" text NOT NULL, "list_price" int8, "list_psbt" text, "asking_asset" text, "views" integer NOT NULL DEFAULT 0, "slug" text NOT NULL, "address" text NOT NULL, "bid_id" uuid NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("artwork_id") REFERENCES "public"."artworks"("id") ON UPDATE restrict ON DELETE cascade, FOREIGN KEY ("bid_id") REFERENCES "public"."transactions"("id") ON UPDATE restrict ON DELETE set null, UNIQUE ("id"), UNIQUE ("bid_id"));
CREATE EXTENSION IF NOT EXISTS pgcrypto;
